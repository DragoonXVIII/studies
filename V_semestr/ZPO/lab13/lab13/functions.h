#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include<vector>
#include<string>
#include<iostream>

using namespace std;

bool isEven(int n);
int sign(int n);
vector<int> difference(vector<int> vec);
void show(string line);

#endif // FUNCTIONS_H
